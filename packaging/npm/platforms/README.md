# PortBoard platform package

This internal package carries one native executable used by
`@leonpure/portboard`. Install the main package instead:

```bash
npm install --global @leonpure/portboard
```
